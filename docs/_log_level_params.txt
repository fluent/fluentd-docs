#### log_level option (Fluentd v0.10.43 and above)

The `log_level` option allows the user to set different levels of logging for each plugin. Supported log levels are, `fatal`, `error`, `warn`, `info`, `debug` and `trace`.

See [logging article](logging) for more details

